// Auto-generated by @typeglot/compiler
// Do not edit manually

import type { Locale } from './index.js';

let currentLocale: Locale = 'en';
let messages: Record<string, string> = {};

/**
 * Set the current locale
 */
export function setLocale(locale: Locale): void {
  currentLocale = locale;
}

/**
 * Get the current locale
 */
export function getLocale(): Locale {
  return currentLocale;
}

/**
 * Load messages for a locale
 */
export function loadMessages(localeMessages: Record<string, string>): void {
  messages = localeMessages;
}

/**
 * Format a message with parameters
 */
function formatMessage(template: string, params: Record<string, string | number>): string {
  return template.replace(/\{(\w+)\}/g, (_, key) => {
    const value = params[key];
    return value !== undefined ? String(value) : `{${key}}`;
  });
}

/**
 * @default "Welcome to TypeGlot!"
 */
export function welcome(): string {
  return messages['welcome'] ?? `Welcome to TypeGlot!`;
}

/**
 * @default "Hello, {name}!"
 */
export function hello(params: { name: string }): string {
  const template = messages['hello'] ?? `Hello, {name}!`;
  return formatMessage(template, params);
}

/**
 * @default "{count, plural, one {# item} other {# items}}"
 */
export function items_count(): string {
  return messages['items_count'] ?? `{count, plural, one {# item} other {# items}}`;
}

/**
 * @default "Checkout Now"
 */
export function checkout_now(): string {
  return messages['checkout_now'] ?? `Checkout Now`;
}

/**
 * @default "Save Settings"
 */
export function save_settings(): string {
  return messages['save_settings'] ?? `Save Settings`;
}

/**
 * @default "Your profile has been updated successfully."
 */
export function profile_updated(): string {
  return messages['profile_updated'] ?? `Your profile has been updated successfully.`;
}

/**
 * All typed translation functions
 */
export const m = {
  welcome,
  hello,
  items_count,
  checkout_now,
  save_settings,
  profile_updated,
} as const;
